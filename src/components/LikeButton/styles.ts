import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    align-items: center;
    text-align: center;
    gap: 5px;

    svg {
        cursor: pointer;
        color: #ff0000;
        font-size: 18px;
    }
`;
